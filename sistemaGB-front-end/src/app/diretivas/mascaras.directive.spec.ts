import { MascarasDirective } from './mascaras.directive';

describe('MascarasDirective', () => {
  it('should create an instance', () => {
    const directive = new MascarasDirective();
    expect(directive).toBeTruthy();
  });
});
